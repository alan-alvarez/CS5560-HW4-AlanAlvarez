export class Cat {
  /* _id?: string;
  name?: string;
  weight?: number;
  age?: number; */
  
  _id?: 			string;
  catdomain?: 		string;
  catusername?: 	string;
  catpassword?: 	string;
  user?: 			string;
  
  //show?:			boolean; //used to show or hide the combination in the form
  //could not set it back to false when exiting page... going to map it manually?????
}
